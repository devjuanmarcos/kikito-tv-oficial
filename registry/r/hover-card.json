{
  "name": "hover-card",
  "version": "1.1.0",
  "title": "Hover Card",
  "type": "registry:component",
  "description": "Rich preview card shown on hover.",
  "group": "display",
  "status": "live",
  "dependencies": [],
  "registryDependencies": ["tooltip"],
  "tailwind": {
    "requires": ["kikitocn-tokens"]
  },
  "docs": "https://cn.kikito.tv/components/hover-card",
  "files": [
    {
      "path": "components/ui/cn/hover-card/HoverCard.tsx",
      "type": "registry:component",
      "target": "components/ui/cn/hover-card/HoverCard.tsx",
      "content": "\"use client\";\r\n/**\r\n * HoverCard — backward-compat wrapper.\r\n * Absorbed by the Tooltip Super component (`<Tooltip variant=\"card\" />`). Kept so\r\n * existing imports of `HoverCard` keep working; new code should use Tooltip directly.\r\n */\r\nimport type React from \"react\";\r\n\r\nimport { Tooltip } from \"@/components/ui/cn/tooltip\";\r\nimport type { HoverCardSide, HoverCardAlign } from \"@/components/ui/cn/tooltip/Tooltip\";\r\n\r\nexport type { HoverCardSide, HoverCardAlign } from \"@/components/ui/cn/tooltip/Tooltip\";\r\n\r\nexport interface HoverCardProps {\r\n  content: React.ReactNode;\r\n  side?: HoverCardSide;\r\n  align?: HoverCardAlign;\r\n  openDelay?: number;\r\n  closeDelay?: number;\r\n  children: React.ReactElement;\r\n  className?: string;\r\n  style?: React.CSSProperties;\r\n}\r\n\r\nexport function HoverCard({ children, ...props }: HoverCardProps) {\r\n  return (\r\n    <Tooltip variant=\"card\" {...props}>\r\n      {children}\r\n    </Tooltip>\r\n  );\r\n}\r\n"
    },
    {
      "path": "components/ui/cn/hover-card/index.ts",
      "type": "registry:file",
      "target": "components/ui/cn/hover-card/index.ts",
      "content": "export { HoverCard } from './HoverCard'\nexport type { HoverCardProps, HoverCardSide, HoverCardAlign } from './HoverCard'\n"
    }
  ]
}
