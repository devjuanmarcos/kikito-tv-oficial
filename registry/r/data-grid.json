{
  "name": "data-grid",
  "version": "1.1.0",
  "title": "Data Grid",
  "type": "registry:component",
  "description": "High-performance editable data grid with virtualization.",
  "group": "data",
  "status": "live",
  "dependencies": [],
  "registryDependencies": ["table"],
  "tailwind": {
    "requires": ["kikitocn-tokens"]
  },
  "docs": "https://cn.kikito.tv/components/data-grid",
  "files": [
    {
      "path": "components/ui/cn/data-grid/data-grid.types.ts",
      "type": "registry:file",
      "target": "components/ui/cn/data-grid/data-grid.types.ts",
      "content": "﻿import type React from 'react'\r\n\r\nexport interface DataGridColumn<T = Record<string, unknown>> {\r\n  key: string\r\n  header: string\r\n  width?: number | string\r\n  sortable?: boolean\r\n  render?: (row: T, rowIndex: number) => React.ReactNode\r\n  align?: 'left' | 'center' | 'right'\r\n}\r\n\r\nexport interface DataGridProps<T = Record<string, unknown>> {\r\n  columns: DataGridColumn<T>[]\r\n  rows: T[]\r\n  getRowKey?: (row: T, index: number) => string\r\n  selectable?: boolean\r\n  selectedKeys?: string[]\r\n  onSelectionChange?: (keys: string[]) => void\r\n  stickyHeader?: boolean\r\n  striped?: boolean\r\n  className?: string\r\n  style?: React.CSSProperties\r\n}\r\n"
    },
    {
      "path": "components/ui/cn/data-grid/DataGrid.tsx",
      "type": "registry:component",
      "target": "components/ui/cn/data-grid/DataGrid.tsx",
      "content": "\"use client\";\r\n\r\nimport { DataTable } from \"@/components/ui/cn/table/Table\";\r\n\r\nimport type { DataGridProps } from \"./data-grid.types\";\r\n\r\n/**\r\n * DataGrid — backward-compat wrapper over the Super DataTable (`variant=\"grid\"`).\r\n * Visual and API identical to the original DataGrid.\r\n */\r\nexport function DataGrid<T = Record<string, unknown>>(props: DataGridProps<T>) {\r\n  return <DataTable variant=\"grid\" {...(props as DataGridProps<Record<string, unknown>>)} />;\r\n}\r\n"
    },
    {
      "path": "components/ui/cn/data-grid/index.ts",
      "type": "registry:file",
      "target": "components/ui/cn/data-grid/index.ts",
      "content": "﻿export { DataGrid } from './DataGrid'\r\nexport type { DataGridProps, DataGridColumn } from './data-grid.types'\r\n"
    }
  ]
}
