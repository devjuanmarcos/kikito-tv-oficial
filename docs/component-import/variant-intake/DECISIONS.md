# Decisões do usuário — itens ⏳ do `SURVEY.md`

Validado item a item em 2026-08-29, antes de iniciar qualquer código. Cada linha vira uma tarefa de implementação; nenhuma foi implementada ainda.

| #   | Categoria                 | Decisão                                | Escopo                                                                                                                                                                                                                                                                           |
| --- | ------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `dialog` (Modal)          | ✅ Trazer — **direcionais**            | `slide-in-from-{top,bottom,left,right}` como opção nova de entrada, junto do `scaleInDown` atual. Presets novos em `@/lib/motion` primeiro (Gate 8), depois consumir no `Modal`. Zoom variants (50/150) NÃO entram — só direcionais.                                             |
| 2   | `button`                  | ✅ Trazer — **as 3 famílias**          | `ButtonEffect` novo com 3 valores: reveal de ícone (`button-01/19`), preenchimento radial a partir do cursor (`button-16/17`), shine sweep (`button-02/03`, mesma técnica do `text-effect` shine).                                                                               |
| 3   | `radio-group`             | ✅ Trazer — **variant="card"**         | `RadioGroupOption` ganha campos opcionais (`icon`, `description`, `price`), novo `variant="card"` no `Radio`. Aceito o overlap conceitual com `pricing-card`/`price-table` — são componentes diferentes (seleção de formulário vs. apresentação de planos), overlap é aceitável. |
| 4   | `spinner`                 | ✅ Trazer — **variant "orbital"**      | Núcleo pulsante + satélite orbitando, SVG+CSS puro, novo valor em `SpinnerVariant`.                                                                                                                                                                                              |
| 5   | `calendar`                | ✅ Trazer — **no Calendar standalone** | `Calendar` (não só `DatePicker`) ganha `mode="range"`/`"multiple"`, mesmo padrão de union discriminada já usado no `DatePicker`.                                                                                                                                                 |
| 6   | `combobox` (Select)       | ✅ Trazer — **ambos**                  | Modo `combobox` do `Select` ganha `SelectGroup` (grupos) **e** slot de ícone/avatar/bandeira por opção — paridade completa com os modos `single`/`rich`.                                                                                                                         |
| 7   | `code-block`              | ✅ Trazer — **abas por arquivo**       | `MultiFileCodeBlock`-like: abas trocando entre arquivos diferentes. Abas por linguagem (curl/JS/Python mesmo conteúdo) NÃO entra nesta rodada.                                                                                                                                   |
| 8   | `dropdown-menu`           | ✅ Trazer — **prop `header`**          | `header?: ReactNode` no topo de `DropdownMenuProps` (não um novo tipo dentro de `MenuEntry[]`) — mais simples e livre, renderiza antes dos grupos.                                                                                                                               |
| 9   | `line-chart`              | ✅ Trazer — **ReferenceLine**          | Linha horizontal de meta/média com rótulo, implementada nativa (SVG extra, sem `recharts`).                                                                                                                                                                                      |
| 10  | `progress`                | ✅ Trazer — **modo indeterminate**     | Progresso auto-incrementando com jitter + mensagens de status rotativas ("loading fake"), novo modo na `Progress`.                                                                                                                                                               |
| 11  | `area-chart`/`line-chart` | ✅ Trazer — **interpolação "step"**    | `interpolation="step"` (curva em degrau) compartilhado entre os dois tipos, além do que já existe (reta/suave).                                                                                                                                                                  |
| 12  | `badge`                   | ✅ Trazer — **status glow**            | Badge de status assíncrono: glow ambiente + ícone com pop-in + rótulo revelado letra-por-letra via `motion` (`staggerContainer`). Cuidado de a11y: spans por letra `aria-hidden`, texto completo acessível no wrapper.                                                           |

## Resultado

**Todos os 12 itens ⏳ foram aprovados** — nenhum descartado nesta rodada. Somados aos 11 ✅ mecânicos do `SURVEY.md`, ficam **23 itens de implementação** na fila total.

Nenhuma prioridade/ordem foi definida ainda — decidir ordem de execução separadamente (por componente, por esforço, ou por valor de showcase).
