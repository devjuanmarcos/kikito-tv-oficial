{
  "name": "popover",
  "version": "1.1.0",
  "title": "Popover",
  "type": "registry:component",
  "description": "Positioned popover with arrow.",
  "group": "overlays",
  "status": "live",
  "dependencies": [],
  "registryDependencies": [
    "tooltip"
  ],
  "tailwind": {
    "requires": [
      "kikitocn-tokens"
    ]
  },
  "docs": "https://cn.kikito.tv/components/popover",
  "files": [
    {
      "path": "components/ui/cn/popover/index.ts",
      "type": "registry:file",
      "target": "components/ui/cn/popover/index.ts",
      "content": "export { Popover } from \"./Popover\";\nexport type { PopoverProps, PopoverPlacement } from \"./popover.types\";\n"
    },
    {
      "path": "components/ui/cn/popover/Popover.tsx",
      "type": "registry:component",
      "target": "components/ui/cn/popover/Popover.tsx",
      "content": "\"use client\";\n/**\n * Popover — backward-compat wrapper.\n * Absorbed by the Tooltip Super component (`<Tooltip trigger=\"click\" />`). Kept so\n * existing imports of `Popover` keep working; new code should use Tooltip directly.\n */\nimport { Tooltip } from \"@/components/ui/cn/tooltip\";\n\nimport type { PopoverProps } from \"./popover.types\";\n\nexport function Popover({ children, ...props }: PopoverProps) {\n  return (\n    <Tooltip trigger=\"click\" {...props}>\n      {children}\n    </Tooltip>\n  );\n}\n"
    },
    {
      "path": "components/ui/cn/popover/popover.types.ts",
      "type": "registry:file",
      "target": "components/ui/cn/popover/popover.types.ts",
      "content": "import type React from \"react\";\n\nimport type { PopoverPlacement } from \"@/components/ui/cn/tooltip/tooltip.types\";\n\nexport type { PopoverPlacement };\n\nexport interface PopoverProps {\n  content?: React.ReactNode;\n  placement?: PopoverPlacement;\n  title?: string;\n  description?: string;\n  footer?: React.ReactNode;\n  showClose?: boolean;\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  children: React.ReactElement;\n}\n"
    }
  ]
}