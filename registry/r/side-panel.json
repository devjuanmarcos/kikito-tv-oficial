{
  "name": "side-panel",
  "version": "1.1.0",
  "title": "Side Panel",
  "type": "registry:component",
  "description": "Resizable side panel layout.",
  "group": "navigation",
  "status": "live",
  "dependencies": [],
  "registryDependencies": ["modal"],
  "tailwind": {
    "requires": ["kikitocn-tokens"]
  },
  "docs": "https://cn.kikito.tv/components/side-panel",
  "files": [
    {
      "path": "components/ui/cn/side-panel/index.ts",
      "type": "registry:file",
      "target": "components/ui/cn/side-panel/index.ts",
      "content": "export { SidePanel } from './SidePanel'\r\nexport type { SidePanelProps } from './side-panel.types'\r\n"
    },
    {
      "path": "components/ui/cn/side-panel/side-panel.types.ts",
      "type": "registry:file",
      "target": "components/ui/cn/side-panel/side-panel.types.ts",
      "content": "import type React from 'react'\r\n\r\nexport interface SidePanelProps {\r\n  panel: React.ReactNode\r\n  children: React.ReactNode\r\n  open?: boolean\r\n  defaultOpen?: boolean\r\n  onOpenChange?: (open: boolean) => void\r\n  side?: 'left' | 'right'\r\n  panelWidth?: number\r\n  collapsedWidth?: number\r\n  className?: string\r\n  style?: React.CSSProperties\r\n}\r\n"
    },
    {
      "path": "components/ui/cn/side-panel/SidePanel.tsx",
      "type": "registry:component",
      "target": "components/ui/cn/side-panel/SidePanel.tsx",
      "content": "\"use client\";\r\n\r\nimport React from \"react\";\r\n\r\nimport { Modal } from \"@/components/ui/cn/modal\";\r\n\r\nimport type { SidePanelProps } from \"./side-panel.types\";\r\n\r\n/**\r\n * SidePanel — backward-compat wrapper over the Super `Modal` (variant=\"panel\").\r\n * Render/behavior absorbed verbatim into Modal; legacy `side` maps to\r\n * Modal's `panelSide`. SidePanel has no dismiss action, so `onClose` is a no-op.\r\n */\r\nexport function SidePanel({ side = \"left\", ...rest }: SidePanelProps) {\r\n  return <Modal variant=\"panel\" panelSide={side} onClose={() => {}} {...rest} />;\r\n}\r\n"
    }
  ]
}
