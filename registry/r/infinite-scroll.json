{
  "name": "infinite-scroll",
  "version": "1.1.0",
  "title": "Infinite Scroll",
  "type": "registry:component",
  "description": "Load-on-scroll data fetching.",
  "group": "layout",
  "status": "live",
  "dependencies": [],
  "registryDependencies": ["utils"],
  "tailwind": {
    "requires": ["kikitocn-tokens"]
  },
  "docs": "https://cn.kikito.tv/components/infinite-scroll",
  "files": [
    {
      "path": "components/ui/cn/infinite-scroll/index.ts",
      "type": "registry:file",
      "target": "components/ui/cn/infinite-scroll/index.ts",
      "content": "export { InfiniteScroll } from './InfiniteScroll'\r\nexport type { InfiniteScrollProps } from './infinite-scroll.types'\r\n"
    },
    {
      "path": "components/ui/cn/infinite-scroll/infinite-scroll.types.ts",
      "type": "registry:file",
      "target": "components/ui/cn/infinite-scroll/infinite-scroll.types.ts",
      "content": "import type { ReactNode } from \"react\";\n\nexport interface InfiniteScrollProps {\n  children: ReactNode;\n  onLoadMore: () => void;\n  hasMore?: boolean;\n  isLoading?: boolean;\n  threshold?: number;\n  loader?: ReactNode;\n  endMessage?: ReactNode;\n  className?: string;\n  style?: React.CSSProperties;\n}\n"
    },
    {
      "path": "components/ui/cn/infinite-scroll/InfiniteScroll.tsx",
      "type": "registry:component",
      "target": "components/ui/cn/infinite-scroll/InfiniteScroll.tsx",
      "content": "\"use client\";\n\nimport { useEffect, useRef } from \"react\";\n\nimport { cn } from \"@/lib/utils\";\n\nimport type { InfiniteScrollProps } from \"./infinite-scroll.types\";\n\nexport function InfiniteScroll({\n  children,\n  onLoadMore,\n  hasMore = true,\n  isLoading = false,\n  loader,\n  endMessage,\n  threshold = 0.1,\n  className,\n  style,\n}: InfiniteScrollProps) {\n  const sentinelRef = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    const sentinel = sentinelRef.current;\n    if (!sentinel) return;\n\n    const observer = new IntersectionObserver(\n      ([entry]) => {\n        if (entry.isIntersecting && hasMore && !isLoading) {\n          onLoadMore();\n        }\n      },\n      { threshold }\n    );\n\n    observer.observe(sentinel);\n    return () => observer.disconnect();\n  }, [hasMore, isLoading, onLoadMore, threshold]);\n\n  return (\n    <div className={cn(\"flex flex-col\", className)} style={style}>\n      {children}\n\n      {isLoading && (\n        <div role=\"status\" aria-label=\"Loading more\" className=\"flex justify-center py-(--spacing-lg)\">\n          {loader ?? (\n            <div\n              aria-hidden=\"true\"\n              className=\"w-6 h-6 border-2 border-rule border-t-patina rounded-full animate-spin\"\n            />\n          )}\n        </div>\n      )}\n\n      {!hasMore && endMessage && (\n        <div role=\"status\" className=\"flex justify-center py-(--spacing-lg) text-body-callout text-faint\">\n          {endMessage}\n        </div>\n      )}\n\n      <div ref={sentinelRef} className=\"h-px\" />\n    </div>\n  );\n}\n"
    }
  ]
}
